/** Kept for deployment compatibility; the site uses a compact custom design system in src/index.css. */
export default { content: ['./index.html', './src/**/*.{js,ts,jsx,tsx}'], theme: { extend: {} }, plugins: [] }
