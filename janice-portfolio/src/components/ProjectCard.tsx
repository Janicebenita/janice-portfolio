import { ArrowDown, ArrowUpRight, Github, Check } from 'lucide-react'; import { motion } from 'framer-motion'; import type { Project } from '../data/projects'; import { ProjectVisual } from './ProjectVisual'
export function ProjectCard({project,index}:{project:Project;index:number}){return <motion.article className={`project-card case-study glass ${project.featured?'featured':''}`} initial={{opacity:0,y:24}} whileInView={{opacity:1,y:0}} viewport={{once:true,margin:'-80px'}} transition={{duration:.5}}><div className="project-image"><ProjectVisual project={project} index={index}/><span>0{index+1}</span></div><div className="project-body"><p className="project-subtitle">{project.subtitle}</p><h3>{project.title}</h3><p>{project.description}</p><div className="case-grid"><div><span>PROBLEM</span><p>{project.problem}</p></div><div><span>RESULT</span><p>{project.result}</p></div></div><div className="architecture"><span>ARCHITECTURE</span><div>{project.architecture.map((step,stepIndex)=><div key={step}><strong>{step}</strong>{stepIndex<project.architecture.length-1&&<ArrowDown/>}</div>)}</div></div><ul className="highlights">{project.highlights.map(h=><li key={h}><Check size={14}/>{h}</li>)}</ul><div className="tags">{project.tags.map(t=><span key={t}>{t}</span>)}</div><div className="project-links">{project.github&&<a href={project.github} target="_blank" rel="noreferrer"><Github size={17}/> Repository</a>}{project.repositoryPending&&<span className="link-placeholder" aria-label="EndoXAI repository link is not yet available"><Github size={17}/> Repository link coming soon</span>}{project.demo&&<a href={project.demo}>Live demo <ArrowUpRight size={17}/></a>}</div></div></motion.article>}
