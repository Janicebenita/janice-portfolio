import { ArrowDownRight, Download, Github, Linkedin, Mail } from 'lucide-react'
import { motion } from 'framer-motion'
import { achievements, profile } from '../data/profile'
import { assetPath } from '../lib/asset'

export function Hero() {
  return <section id="top" className="hero container"><motion.div className="hero-copy" initial={{ opacity: 0, y: 24 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: .65 }}><p className="availability"><span /> Chennai, India · Open to opportunities</p><h1>Building AI systems that are <em>explainable, verifiable,</em> and useful.</h1><p className="hero-subtitle">AI/ML Engineer focused on Explainable AI, Computer Vision, AI Agents, and Data Intelligence.</p><p className="hero-summary">{profile.summary}</p><div className="hero-actions"><a className="button" href="#projects">View projects <ArrowDownRight size={18} /></a><a className="button secondary" href={assetPath('resume/Janice_Benita_Resume.pdf')} target="_blank" rel="noreferrer"><Download size={18} /> Download resume</a></div><div className="social-row"><a href={profile.github} target="_blank" rel="noreferrer"><Github /> GitHub</a><a href={profile.linkedin} target="_blank" rel="noreferrer"><Linkedin /> LinkedIn</a><a href={`mailto:${profile.email}`}><Mail /> Email</a></div></motion.div><motion.div className="hero-visual" initial={{ opacity: 0, scale: .96 }} animate={{ opacity: 1, scale: 1 }} transition={{ duration: .7, delay: .12 }}><div className="portrait-shell"><div className="portrait-grid" /><div className="portrait-monogram">JB</div><div className="orbit orbit-a" /><div className="orbit orbit-b" /><div className="role-chip">AI / ML<br /><strong>ENGINEER</strong></div></div><p className="visual-caption"><span>01</span> Engineering intelligence with evidence and human oversight.</p></motion.div><div className="metric-grid">{achievements.map(([number, label], index) => <motion.div className="metric" key={label} initial={{ opacity: 0, y: 15 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} transition={{ delay: index * .06 }}><strong>{number}</strong><span>{label}</span></motion.div>)}</div></section>
}
