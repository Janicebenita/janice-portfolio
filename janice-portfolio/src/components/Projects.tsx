import { projects } from '../data/projects'; import { SectionHeading } from './SectionHeading'; import { ProjectCard } from './ProjectCard'
export function Projects(){return <section id="projects" className="section container"><SectionHeading eyebrow="04 / Engineering case studies" title="Complex systems, explained clearly." intro="Each case study connects the engineering problem, system architecture, implementation choices, and verified result."/><div className="projects-grid">{projects.map((p,i)=><ProjectCard project={p} index={i} key={p.title}/>)}</div></section>}
