import { ArrowUpRight, BookOpen } from 'lucide-react'; import { publications } from '../data/publications'; import { SectionHeading } from './SectionHeading'
export function Publications(){return <section id="research" className="section container"><SectionHeading eyebrow="04 / Research" title="Research grounded in applied intelligence."/><div className="research-grid">{publications.map((p,i)=><article className="research-card glass" key={p.title}><div className="research-top"><span className={`status ${p.status.toLowerCase()}`}>{p.status}</span><BookOpen/></div><span className="paper-number">PAPER / 0{i+1}</span><h3>{p.title}</h3><p>{p.journal}</p><div className="paper-meta"><span>{p.volume}</span><span>{p.year}</span></div>{p.link ? <a href={p.link} target="_blank" rel="noreferrer">Journal / DOI <ArrowUpRight size={16}/></a> : <span className="link-placeholder">DOI / journal link pending</span>}</article>)}</div></section>}
