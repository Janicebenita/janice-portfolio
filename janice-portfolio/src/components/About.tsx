import { BrainCircuit, ScanSearch, Bot, ChartNoAxesCombined, PanelsTopLeft, Gauge } from 'lucide-react'
import { focusAreas } from '../data/profile'; import { SectionHeading } from './SectionHeading'
const icons=[BrainCircuit,ScanSearch,Bot,ChartNoAxesCombined,PanelsTopLeft,Gauge]
export function About(){return <section id="about" className="section container"><SectionHeading eyebrow="01 / About" title="Engineering intelligence, not just models."/><div className="about-layout"><p className="lead">Janice designs AI-driven systems that connect <span>data, decision-making, explainability,</span> and human oversight. Her work includes autonomous software reliability, clinical AI, infrastructure computer vision, and engineering quality intelligence.</p><div className="focus-grid">{focusAreas.map((f,i)=>{const Icon=icons[i];return <div className="focus-card" key={f}><Icon size={20}/><span>{f}</span></div>})}</div></div></section>}
